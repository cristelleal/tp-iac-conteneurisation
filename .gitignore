# Variables d'environnement (contient des mots de passe)
.env

# Terraform : fichiers générés, ne pas committer
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
*.tfstate.lock.info
terraform.tfvars

# Fichiers de backup créés par sed
*.bak

# Certificats TLS (générés localement, ne pas committer les clés privées)
docker/nginx/certs/

# macOS
.DS_Store
